<?php $styles = explode(',', STYLES) ?>
<?php if (isset($styles) && is_array($styles)): ?>
<?php foreach ($styles as $style): ?>
<link rel="stylesheet" href="<?php
if (preg_match('@//@', $style)) {
    echo $style;
} else {
    echo GOOGLEPLUS_APP_ROOT . $style;
}
?>">
<?php endforeach; ?>
<?php endif; ?>
