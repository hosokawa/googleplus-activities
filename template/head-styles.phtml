<?php if ($styles = $this->config('header_styles')): ?>
<?php foreach ($styles as $style): ?>
<link rel="stylesheet" href="<?php
if (preg_match('@//@', $style)) {
    echo $style;
} else {
    echo $this->config('SERVER.HTTP_ROOT') . $style;
}
?>">
<?php endforeach; ?>
<?php endif; ?>
