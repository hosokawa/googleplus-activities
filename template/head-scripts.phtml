<?php if ($scripts = $this->config('header_scripts')): ?>
<?php foreach ($scripts as $script): ?>
<script src="<?php
if (preg_match('@//@', $script)) {
    echo $script;
} else {
    echo $this->config('SERVER.HTTP_ROOT') . $script;
}
?>"></script>
<?php endforeach; ?>
<?php endif; ?>
